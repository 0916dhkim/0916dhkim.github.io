import { style } from "@vanilla-extract/css";

export const root = style({
  minWidth: "100vw",
  minHeight: "100vh",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  padding: "4rem",
});
